rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    match /community_map_cloud_data/{document=**} {
      allow read, write: if request.auth != null
                         && request.auth.uid == "I1AGzKoXMTQBEXTWdkLioDbl7Xs2";
    }
  }
}
